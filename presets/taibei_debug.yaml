# Debug preset — allow disjoint OSM/terrain for partial runs

extends:
  - taibei_ue.yaml

scene:
  id_pattern: "taibei_debug_{timestamp}"

frame:
  extent:
    allow_disjoint: true

inputs:
  dem:
    optional: true

pipeline:
  - step: terrain
    enabled: false

runtime:
  resume: true
