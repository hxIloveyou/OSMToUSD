把输入数据放到本目录（子目录可按类型分开放，也可直接丢在 data 根下）：

  osm/            OpenStreetMap：.osm / .osm.pbf
  dem/            高程：GeoTIFF / PNG+meta
  imagery/        正射影像：GeoTIFF / PNG
  assets/         资产库（可不全；生成时会补充）
  descriptions/   场景描述 JSON

放好后在对话里说一声即可开始生成。
